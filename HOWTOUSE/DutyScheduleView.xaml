<UserControl x:Class="HOWTOUSE.DutyScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="Panel" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D8E0EA" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="26" />
        </Style>
        <Style x:Key="CalendarComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="#0F172A" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Padding="{TemplateBinding Padding}" CornerRadius="6" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#EAF2FF" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#DBEAFE" />
                                <Setter Property="Foreground" Value="#1D4ED8" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CalendarComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="38" />
            <Setter Property="Background" Value="#F8FAFC" />
            <Setter Property="BorderBrush" Value="#E2E8F0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,0" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#0F172A" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource CalendarComboBoxItem}" />
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource Panel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Content="‹" Width="38" Height="38" Background="Transparent" BorderThickness="0" FontSize="24" Click="PreviousMonthButton_Click" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <ComboBox x:Name="YearComboBox" Style="{StaticResource CalendarComboBox}" Width="92" SelectionChanged="CalendarSelectionChanged" />
                    <ComboBox x:Name="MonthComboBox" Style="{StaticResource CalendarComboBox}" Width="64" Margin="8,0,0,0" SelectionChanged="CalendarSelectionChanged" />
                </StackPanel>
                <Button Grid.Column="2" Content="›" Width="38" Height="38" Background="Transparent" BorderThickness="0" FontSize="24" Click="NextMonthButton_Click" />
            </Grid>

            <UniformGrid Grid.Row="1" Columns="7" Margin="80,22,80,8">
                <TextBlock Text="Su" HorizontalAlignment="Center" Foreground="#8A8F98" />
                <TextBlock Text="Mo" HorizontalAlignment="Center" Foreground="#8A8F98" />
                <TextBlock Text="Tu" HorizontalAlignment="Center" Foreground="#8A8F98" />
                <TextBlock Text="We" HorizontalAlignment="Center" Foreground="#8A8F98" />
                <TextBlock Text="Th" HorizontalAlignment="Center" Foreground="#8A8F98" />
                <TextBlock Text="Fr" HorizontalAlignment="Center" Foreground="#8A8F98" />
                <TextBlock Text="Sa" HorizontalAlignment="Center" Foreground="#8A8F98" />
            </UniformGrid>

            <ItemsControl x:Name="CalendarItemsControl" Grid.Row="2" Margin="80,0,80,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="7" Rows="6" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="96" Height="66" CornerRadius="8" Margin="4" Padding="8,6" Background="{Binding Background}" HorizontalAlignment="Center">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding DayText}" Foreground="{Binding DayForeground}" FontSize="17" FontWeight="SemiBold" />
                                <TextBlock Grid.Row="1" Text="{Binding DutyOfficerName}" Foreground="{Binding DutyOfficerForeground}" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
